<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Logo -->
        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/logoapp"
            android:contentDescription="Logo aplikasi Body Fat Prediction" />

        <!-- Judul -->
        <TextView
            android:id="@+id/titleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Body Fat Prediction"
            android:textSize="24sp"
            android:textColor="@android:color/black"
            android:textStyle="bold"
            android:layout_marginTop="16dp" />

        <!-- Deskripsi -->
        <TextView
            android:id="@+id/descriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Aplikasi ini digunakan untuk memprediksi persentase lemak tubuh (body fat percentage) berdasarkan data antropometri pria."
            android:textAlignment="center"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            android:layout_marginTop="24dp" />

        <!-- Tombol MASUK -->
        <Button
            android:id="@+id/buttonMasuk"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="MASUK"
            android:layout_marginTop="32dp"
            android:textStyle="bold"
            android:backgroundTint="#4A4AFF"
            android:textColor="@android:color/white"
            android:layout_marginBottom="32dp"
            android:elevation="6dp"
            android:background="@drawable/rounded_button" />

    </LinearLayout>
</ScrollView>
